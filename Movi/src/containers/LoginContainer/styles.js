import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  stl_login_input: {
    marginBottom: 20
  },
  stl_password_input: {
    marginBottom: 30
  },
  txt_container: {
    paddingBottom: 10,
    alignItems:'flex-end'
  }
  
});