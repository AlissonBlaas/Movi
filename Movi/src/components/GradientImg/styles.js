import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  stl_background_gradient: {
    position: 'absolute',
    right: 0,
    left: 0,
    bottom: 0,
    top: 0
  }
});